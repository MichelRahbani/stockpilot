{
  "name": "stockpilot",
  "version": "0.1.0",
  "private": true,
  "description": "StockPilot local frontend plus API data gateway.",
  "scripts": {
    "start": "node server.js",
    "api": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
